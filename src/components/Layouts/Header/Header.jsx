import { Section } from "../../UI/Section/Section"
import { Navbar } from "../../UI/Navbar/Navbar"
import "./Header.css"


export const Header = () => {
    return (  
        <header className="header">
            
        <Navbar/>
        <Section/>
        </header>
    )
}
